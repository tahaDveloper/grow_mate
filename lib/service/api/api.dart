import 'package:hive/hive.dart';

import '../../model/taskModel/task_model.dart';

class Api {

  // Map<String, List<Task>> groupTasksByCategory(Box<Task> taskBox) {
  //   final Map<String, List<Task>> categoryMap = {};
  //
  //   for (var i = 0; i < taskBox.length; i++) {
  //     final task = taskBox.getAt(i);
  //     if (task != null) {
  //       categoryMap.putIfAbsent(task.category, () => []);
  //       categoryMap[task.category]!.add(task);
  //     }
  //   }
  //
  //   return categoryMap;
  // }

}